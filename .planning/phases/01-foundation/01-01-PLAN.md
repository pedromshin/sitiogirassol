---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/listing.config.ts
  - .planning/phases/01-foundation/deliverables/airbnb-pricing-guide.md
autonomous: true
requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04
  - PRICE-05
  - PRICE-06

must_haves:
  truths:
    - "Website displays R$280/night as the base rate (not R$340)"
    - "Website shows 12% weekly discount and 27% monthly discount (not 33%/50%)"
    - "Website pricing calculations include R$180 cleaning fee"
    - "Owner has a step-by-step guide for setting weekday R$280, weekend R$380, Easter R$600, Labor Day R$500 pricing on Airbnb"
    - "Owner has instructions for adding R$180 cleaning fee and changing discount percentages on Airbnb"
  artifacts:
    - path: "src/data/listing.config.ts"
      provides: "Updated pricing config"
      contains: "nightlyRate: 280"
    - path: ".planning/phases/01-foundation/deliverables/airbnb-pricing-guide.md"
      provides: "Step-by-step Airbnb pricing change instructions"
      contains: "Easter"
  key_links:
    - from: "src/data/listing.config.ts"
      to: "src/lib/pricing.ts"
      via: "calculateTotalPrice reads config.nightlyRate, config.weeklyDiscountPercent, config.monthlyDiscountPercent, config.cleaningFee"
      pattern: "config\\.nightlyRate"
    - from: "src/data/listing.config.ts"
      to: "src/components/sections/CalendarSection.tsx"
      via: "listingConfig.pricing.nightlyRate"
      pattern: "nightlyRate"
---

<objective>
Update the website's pricing configuration to match the new Airbnb pricing strategy and produce a step-by-step guide for the owner to apply matching changes on the Airbnb platform.

Purpose: The current ADR crashed to R$178/night due to excessive discounts (33% weekly, 50% monthly). This plan fixes pricing both on the website (code change) and provides instructions for Airbnb platform changes. Easter (Apr 17-21) is 21 days away -- pricing must be set before those dates get booked at standard rates.

Output: Updated listing.config.ts (auto-deploys via Vercel) + Airbnb pricing guide markdown document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/data/listing.config.ts
@src/lib/pricing.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/listing.config.ts:
```typescript
export interface ListingConfig {
  // ...
  pricing: {
    nightlyRate: number;
    weeklyDiscountPercent: number;
    monthlyDiscountPercent: number;
    cleaningFee?: number;
    serviceFee?: number;
    currency: "BRL";
  };
  // ...
}
```

Current pricing values (lines 132-137):
```typescript
pricing: {
  nightlyRate: 340,
  weeklyDiscountPercent: 33,
  monthlyDiscountPercent: 50,
  currency: "BRL",
},
```

From src/lib/pricing.ts:
```typescript
export function calculateTotalPrice(
  config: ListingConfig["pricing"],
  startDate: Date,
  endDate: Date
): {
  nights: number;
  nightlyRate: number;
  subtotal: number;
  cleaningFee: number;
  serviceFee: number;
  total: number;
  discountApplied: "none" | "weekly" | "monthly";
};
// Line 33: const cleaningFee = config.cleaningFee ?? 0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update listing.config.ts pricing values</name>
  <files>src/data/listing.config.ts</files>
  <read_first>
    - src/data/listing.config.ts (current pricing values at lines 132-137)
    - src/lib/pricing.ts (verify cleaningFee handling at line 33)
  </read_first>
  <action>
Edit the pricing object in src/data/listing.config.ts (lines 132-137) to replace the current values with the new pricing strategy per locked decisions D-01 through D-06:

BEFORE (lines 132-137):
```typescript
pricing: {
  nightlyRate: 340,
  weeklyDiscountPercent: 33,
  monthlyDiscountPercent: 50,
  currency: "BRL",
},
```

AFTER:
```typescript
pricing: {
  nightlyRate: 280,
  weeklyDiscountPercent: 12,
  monthlyDiscountPercent: 27,
  cleaningFee: 180,
  currency: "BRL",
},
```

Specific changes:
- `nightlyRate: 340` -> `nightlyRate: 280` (weekday base rate per D-01; website shows "starting from" lowest rate)
- `weeklyDiscountPercent: 33` -> `weeklyDiscountPercent: 12` (per D-02)
- `monthlyDiscountPercent: 50` -> `monthlyDiscountPercent: 27` (per D-03)
- Add `cleaningFee: 180,` line after monthlyDiscountPercent (per D-04; field already exists in interface as `cleaningFee?: number`)

Do NOT change any other fields in listing.config.ts. The `currency: "BRL"` line stays as-is.

After editing, run `npx tsc --noEmit` to verify the TypeScript compiles without errors. The `cleaningFee` field is already in the `ListingConfig` interface as optional (`cleaningFee?: number`), so adding the value requires no interface changes.
  </action>
  <verify>
    <automated>cd /Users/kaszek/Desktop/sitiogirassol-main && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/data/listing.config.ts contains `nightlyRate: 280`
    - src/data/listing.config.ts contains `weeklyDiscountPercent: 12`
    - src/data/listing.config.ts contains `monthlyDiscountPercent: 27`
    - src/data/listing.config.ts contains `cleaningFee: 180`
    - src/data/listing.config.ts contains `currency: "BRL"`
    - `npx tsc --noEmit` exits with code 0 (no type errors)
    - src/data/listing.config.ts does NOT contain `nightlyRate: 340`
    - src/data/listing.config.ts does NOT contain `weeklyDiscountPercent: 33`
    - src/data/listing.config.ts does NOT contain `monthlyDiscountPercent: 50`
  </acceptance_criteria>
  <done>listing.config.ts has updated pricing values (280/12/27/180) and TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create Airbnb pricing change guide for owner</name>
  <files>.planning/phases/01-foundation/deliverables/airbnb-pricing-guide.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (locked decisions D-01 through D-08)
    - .planning/phases/01-foundation/01-RESEARCH.md (pitfalls 1-3 and 6, Airbnb fee structure)
  </read_first>
  <action>
Create directory `.planning/phases/01-foundation/deliverables/` if it does not exist, then create the file `airbnb-pricing-guide.md` with a step-by-step owner action guide.

The guide must contain these sections in order:

**1. URGENT: Holiday Pricing (do this FIRST)**
- Easter (Apr 17, 18, 19, 20, 21): Set each night to R$600/night (per D-06)
- Labor Day (May 1, 2): Set each night to R$500/night (per D-07)
- Steps: Airbnb Host Dashboard -> Calendar -> Click each date -> Set custom nightly price
- Per D-08: This is a manual override, not a rule -- set each date individually
- Include a table: Date | Day of Week | Price
  - Apr 17 (Thu) | R$600
  - Apr 18 (Fri) | R$600
  - Apr 19 (Sat) | R$600
  - Apr 20 (Sun) | R$600
  - Apr 21 (Mon) | R$600
  - May 1 (Thu) | R$500
  - May 2 (Fri) | R$500

**2. Base Pricing Changes**
- Set weekday (Sun-Thu) base rate to R$280/night (per D-01)
- Set weekend (Fri-Sat) rate to R$380/night (per D-01, D-05)
- Steps: Airbnb Host Dashboard -> Listing -> Pricing -> Base price
- Note: Airbnb may not have separate weekday/weekend base prices in all regions. If only one base price field: set to R$280 and use "Custom pricing" or "Price rules" to set Fri/Sat to R$380. If no per-day rules available: manually override Fri/Sat in the calendar for the next 3 months.
- Include the math: R$380 weekend is 35.7% above R$280 weekday, satisfying the 25-35% premium target (D-05)

**3. Discount Reductions**
- Weekly discount: Change from 33% to 12% (per D-02)
- Monthly discount: Change from 50% to 27% (per D-03)
- Steps: Airbnb Host Dashboard -> Listing -> Pricing -> Length-of-stay discounts
- Include verification: After changing, check the listing as a guest for 7-night and 28-night stays to confirm displayed prices:
  - 7-night stay: R$280 * 7 * 0.88 = R$1,725 subtotal + R$180 cleaning = R$1,905 total (before Airbnb fees)
  - 28-night stay: R$280 * 28 * 0.73 = R$5,722 subtotal + R$180 cleaning = R$5,902 total (before Airbnb fees)
- Warning per Research Pitfall 6: Check for stacked promotions or "new listing" discounts that may further reduce displayed prices

**4. Cleaning Fee**
- Add cleaning fee: R$180 (per D-04)
- Steps: Airbnb Host Dashboard -> Listing -> Pricing -> Fees -> Cleaning fee
- Purpose: Discourages low-value 1-night stays. The cleaning fee is the same regardless of stay length, so shorter stays have higher per-night effective cost.

**5. Revenue Impact Summary**
Include a table showing expected net revenue (after Airbnb's 16% host fee) for common stay lengths:
- 2-night weekday: (R$280*2 + R$180) * 0.84 = R$621
- 2-night weekend: (R$380*2 + R$180) * 0.84 = R$790
- 7-night mixed (5 weekday + 2 weekend): ((R$280*5 + R$380*2) * 0.88 + R$180) * 0.84 = R$1,752
- Easter 5-night: (R$600*5 + R$180) * 0.84 = R$2,671
- Note per Research Pitfall 2: R$280 rate yields ~R$235 net per night after Airbnb's 16% cut

**6. Verification Checklist**
- [ ] Easter dates (Apr 17-21) show R$600/night in Airbnb calendar
- [ ] Labor Day dates (May 1-2) show R$500/night in Airbnb calendar
- [ ] Base weekday rate is R$280/night
- [ ] Friday and Saturday rates are R$380/night
- [ ] Weekly discount shows 12% (not 33%)
- [ ] Monthly discount shows 27% (not 50%)
- [ ] Cleaning fee shows R$180
- [ ] Preview listing as guest for 2-night stay -- price matches expectations
- [ ] Preview listing as guest for 7-night stay -- weekly discount is ~12%
- [ ] No stacked promotions or "new listing" discounts are active
  </action>
  <verify>
    <automated>test -f /Users/kaszek/Desktop/sitiogirassol-main/.planning/phases/01-foundation/deliverables/airbnb-pricing-guide.md && grep -c "R\$600" /Users/kaszek/Desktop/sitiogirassol-main/.planning/phases/01-foundation/deliverables/airbnb-pricing-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/01-foundation/deliverables/airbnb-pricing-guide.md
    - File contains "Holiday Pricing" section header
    - File contains "R$600" (Easter pricing)
    - File contains "R$500" (Labor Day pricing)
    - File contains "R$280" (weekday base)
    - File contains "R$380" (weekend rate)
    - File contains "12%" (weekly discount)
    - File contains "27%" (monthly discount)
    - File contains "R$180" (cleaning fee)
    - File contains "Apr 17" and "Apr 21" (Easter date range)
    - File contains "May 1" and "May 2" (Labor Day dates)
    - File contains "Verification Checklist" section
    - File contains "16%" (Airbnb host fee mention)
  </acceptance_criteria>
  <done>Complete Airbnb pricing guide exists with all pricing values, holiday dates, step-by-step instructions, verification checklist, and revenue impact summary</done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` -- TypeScript compiles without errors
2. Run `grep "nightlyRate: 280" src/data/listing.config.ts` -- returns match
3. Run `grep "cleaningFee: 180" src/data/listing.config.ts` -- returns match
4. Run `grep "weeklyDiscountPercent: 12" src/data/listing.config.ts` -- returns match
5. Run `grep "monthlyDiscountPercent: 27" src/data/listing.config.ts` -- returns match
6. Verify airbnb-pricing-guide.md exists and contains all 6 sections
7. Run `npm run build` -- website builds successfully with new pricing values
</verification>

<success_criteria>
- listing.config.ts pricing values are 280/12/27/180 (nightlyRate/weekly/monthly/cleaningFee)
- TypeScript compiles cleanly
- Airbnb pricing guide has step-by-step instructions covering all 6 PRICE requirements
- Holiday pricing (Easter R$600, Labor Day R$500) is documented with exact dates
- Revenue impact calculations are included for owner to understand net earnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
