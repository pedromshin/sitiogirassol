/**
 * Forest theme design tokens for JS contexts (e.g. Three.js).
 * Values must match globals.css :root variables.
 */
export const SCENE_COLORS = {
  roof: "#5e7b5b",
  body: "#8ebea8",
  ground: "#3a5a4c",
} as const;
